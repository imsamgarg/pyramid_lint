import 'package:flutter/widgets.dart';

class WrapWithWidgetExample extends StatelessWidget {
  const WrapWithWidgetExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
